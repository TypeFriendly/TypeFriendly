Title: Składnia pliku

----
Przykładowy plik rozdziału wygląda następująco:

<pre name="code">
Title: Tytuł
Opcja1: Wartość
Opcja2: Wartość
ListaUstawień:
 - Wartość 1
 - Wartość 2

----
Treść pliku
</pre>

W ustawieniach umieszczamy kolejne znaczniki w sposób pokazany powyżej. Obowiązkowy jest *Title*, ale jest też kilka innych. Właściwie ich lista zależy bardziej od tego, w jakim formacie wyjściowym będziemy generować dokumentację. Można bardzo łatwo opracować własny i zacząć używać tam nowe znaczniki - zostanie to omówione dalej.

Do formatowania treści używana jest składnia http://michelf.com/projects/php-markdown/  "PHP Markdown Extra" z paroma rozszerzeniami i modyfikacjami. Spotkać się z nią można na wielu stronach internetowych, ponieważ parsery są publicznie dostępne, stąd istnieje szansa, że już ją znasz. Gdyby jednak nie, odwiedź stronę jednego z parserów (np. tę podaną powyżej), by dowiedzieć się więcej. W tym miejscu opiszemy reguły formatowania na swój swój sposób i z uwzględnieniem dokonanych zmian.

Markdown został stworzony, by być zarówno łatwy w użyciu, jak i przyjazny w czytaniu plików źródłowych. Pliki źródłowe dokumentacji TypeFriendly mogą dzięki temu służyć za pomoc nawet bez przepuszczenia ich przez generator. Przyjęte jest założenie, że znaki formatujące powinny jak najmniej rzucać się w oczy czytelnikowi i mieć zbliżone zastosowanie do tego, jakiego już się używa np. w poczcie elektronicznej czy plikach README. Jednak oprócz tego, do użytku dopuszczony jest także HTML i modyfikacje TypeFriendly czasem z tego faktu korzystają.

Paragrafy i nowe linie
======================

W Markdownie obowiązuje zasada, że elementy blokowe oddzielone są od siebie jedną lub większą ilością pustych linii. To samo dotyczy paragrafów. Piszemy tekst, a gdy chcemy rozpocząć nowy, robimy jedną pustą linię przerwy:

<pre name="code">
To jest paragraf 1.

To jest paragraf 2.
</pre>

Aby zrobić zwykłe przełamanie linii, kończymy ją dwoma lub większą ilością spacji i piszemy z powrotem od nowej:

<pre name="code">
To jest tekst z przełamaniem  
linii. Na końcu linjki wyżej są dwie spacje.
</pre>

Nagłówki
========

Markdown obsługuje kilka rodzajów nagłówków. Najprostszym wyjściem jest "podkreślenie" tytułu nagłówka liniami poziomymi lub znakami równości:

<pre name="code">
tekst

Nagłówek 1
==========

tekst

Nagłówek 1.1
------------

tekst
</pre>

Ilość znaków równości czy pauzy nie ma znaczenia.

Alternatywny styl to poprzedzenie tytułu jednym do sześciu znaków **#**:

<pre name="code">
tekst

# nagłówek 1

tekst

## nagłówek 2

tekst

##### nagłówek 6
</pre>

Dopuszczalne jest też zamknięcie nagłówka tymi samymi znakami. Ilość znaków po tytule nie musi się zgadzać z ilością przed:

<pre name="code">
tekst

# nagłówek 1 #

tekst

## nagłówek 2 ######

tekst
</pre>

Listy i cytaty
==============

Markdown pozwala bardzo łatwo tworzyć listy. Dodanie listy numerowanej jest niezwykle proste:

<pre name="code">
Tekst.

1. Element 1
2. Element 2
3. Element 3

Tekst
</pre>

Numery nie grają znaczenia - mogą być w innej kolejności, mogą się powtarzać. Alternatywny sposób to użycie znaku **#** zamiast numerów:

<pre name="code">
Tekst.

# Element 1
# Element 2
# Element 3

Tekst
</pre>

Do tworzenia listy wypunktowanej, linijka musi zaczynać się od znaku **\***, **+** lub **-**:

<pre name="code">
Tekst.

+ Element 1
+ Element 2
+ Element 3

Tekst
</pre>

Markdown dopuszcza umieszczanie cytatów w formie znanej z poczty elektronicznej:

<pre name="code">
Tekst.

> To jest cytat...
> To jest cytat...
> To jest cytat...

Tekst
</pre>

Kod źródłowy
============

Obrazki i multimedia
====================

Formatowanie tekstu
===================

Tabelki
=======




